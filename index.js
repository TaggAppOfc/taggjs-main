import Auth from './auth.js';
import Env from './env.js';

class Client {
    login(token = '') {

    }
}

const Tagg = {
    Client,
    API: {
        Login: Auth.LoginUser
    }
}

export default Tagg;